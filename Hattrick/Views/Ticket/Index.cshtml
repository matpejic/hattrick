
@model Hattrick.Models.ViewsModels.ViewTicket
<!DOCTYPE html>
<html>
<head>

</head>
<body>

    @using (Html.BeginForm("About", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))

    {

        <table class="table">
            <tr>
                <th>
                    @Html.DisplayName("Ticket")
                </th>
                <th>
                    @Html.DisplayName("FootbalGame")
                </th>
                <th>
                    @Html.DisplayName("Time)")
                </th>
                <th>
                    @Html.DisplayName("Coefficient")
                </th>


                <th></th>
            </tr>

            @for (int i = 0; i < Model.ViewTicketList.Count; i++)
            {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.ViewTicketList[i].IdTicket)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.ViewTicketList[i].FootballGame)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.ViewTicketList[i].Time)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => modelItem.ViewTicketList[i].Coefficient)
                </td>
            </tr>

            }
         
 </table>
    }
</body>
</html>